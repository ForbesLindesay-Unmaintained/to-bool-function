{
  "name": "to-bool-function",
  "version": "1.0.1",
  "description": "Convert things to functions for use in some/filter/every etc.",
  "main": "./build/build.js",
  "scripts": {
    "prepublish": "component-install && component-build -s b",
    "test": "component-install && component-build -s b && mocha -R spec"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ForbesLindesay/to-bool-function.git"
  },
  "author": "ForbesLindesay",
  "license": "MIT",
  "devDependencies": {
    "component": "*",
    "mocha": "*",
    "better-assert": "*"
  }
}